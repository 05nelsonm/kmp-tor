<?xml version="1.0" encoding="utf-8"?>
<!--
/*
 * Copyright (c) 2024 Matthew Nelson
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *         https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 **/
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- API 28+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--
    API 34+

    Below are examples of declaring the `foregroundServiceType` and
    associated permission which are required to start as a Foreground
    Service. The types utilized in the examples are, as of now, the only
    types which require no other permissions or runtime prerequisites.

    It is up to you what to declare for the service as it may have an
    impact on your app store review, depending on the type, and this library
    is unwilling to make that choice unilaterally for all its consumers.

    NOTE: `tools:node="merge"` and `android:foregroundServiceType="{type}"`
    MUST be declared within the service tag (as shown in the examples).
    -->


    <!--
    API 34+ (e.g. type: Special Use)
    https://developer.android.com/about/versions/14/changes/fgs-types-required#special-use

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <service
            android:name="io.matthewnelson.kmp.tor.runtime.service.TorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:node="merge">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Your explanation\n
                for special use\n
                here" />
        </service>
    </application>
    -->


    <!--
    API 34+ (e.g. type: Data Sync)
    https://developer.android.com/about/versions/14/changes/fgs-types-required#data-sync

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application>
        <service
            android:name="io.matthewnelson.kmp.tor.runtime.service.TorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
    -->


    <!--
    API 34+ (e.g. type: Media Playback)
    https://developer.android.com/about/versions/14/changes/fgs-types-required#media

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application>
        <service
            android:name="io.matthewnelson.kmp.tor.runtime.service.TorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            tools:node="merge" />
    </application>
    -->

</manifest>
